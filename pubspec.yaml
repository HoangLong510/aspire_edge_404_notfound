name: aspire_edge_404_notfound
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # Firebase (FlutterFire v6)
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1

  # UI/UX & utilities
  google_fonts: ^6.3.1
  http: ^1.5.0
  image_picker: ^1.2.0
  cached_network_image: ^3.3.1
  flutter_animate: ^4.5.0
  lottie: ^2.7.0
  flutter_local_notifications: ^17.2.1
  google_maps_flutter: ^2.6.0
  webview_flutter: ^4.7.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  url_launcher: ^6.2.5
  mailer: ^6.0.1
  iconsax: ^0.0.8

  # iOS-style icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/lottie/
